

export enum tipoInstruccion {
    PRINT,
    DECLARACION,
    ASIGNACION,
    ASIGNACION_COMPUESTA,
    ACTUALIZACION_UNARIA,
    BLOQUE,
    FOR,
    BREAK,
    CONTINUE,
    RETURN,
    FUNCION,
    SWITCH,
    CASE,
    IF,
    ELSE
}